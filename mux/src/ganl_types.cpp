#include "autoconf.h"

#include "ganl_types.h"

// This file implements the GANL network types for TinyMUX integration.
// It includes any non-inline implementation of types defined in ganl_types.h.

namespace ganl {

// Currently, we don't have any non-inline implementations needed for the types in ganl_types.h,
// but this file serves as the compilation unit for linking purposes and could be expanded
// if additional implementation is needed for the network types.

// For example, we might add:
// - Type conversion routines between TinyMUX types and GANL types
// - Helper functions for error handling
// - Static initialization of any global data

} // namespace ganl