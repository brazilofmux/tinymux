/*
 * Compatibility note for the standalone Ragel harness.
 *
 * The single source of truth for these machines now lives in:
 *   ../mux/lib/color_ops.rl
 *
 * ragel/Makefile generates ragel/color_ops.c directly from that file.
 */
